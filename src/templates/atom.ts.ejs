/** Created with RNatify */
import React from "react";
import { Button, StyleSheet } from "react-native";

const styles = StyleSheet.create({
    hello: { padding: 20 }
})

type <%= props.name %>Props = {
    anyProps: string;
}

/**
 * Describe well your component.
 *
 * @param.anyProps Document the props either is a good practice.
 * @return JSX.Element
 */
const <%= props.name %>Component = ({ anyProps }: <%= props.name %>Props): JSX.Element => {
    return (
        <Button style={styles.hello} />
    );
}

export default <%= props.name %>Component;
